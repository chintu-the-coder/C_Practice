#include<stdio.h>
#include<math.h>
int main(){
    int x;
    printf("enter the number:");
    scanf("%d",&x);
    int root = sqrt(x);
    printf("the square root of the number:%d",root);

    return 0;
}